# Cronny Environment Variables
# Copy this file to .env.production (or .env.staging) and fill in the values

# ============================================
# Environment Configuration
# ============================================
CRONNY_ENV=production
DOMAIN_NAME=example.com

# ============================================
# Repository Configuration
# ============================================
# Path to the cronny repository on the server
REPO_PATH=/opt/cronny

# ============================================
# Database Configuration
# ============================================
DB_USER=cronny
DB_PASSWORD=CHANGEME_STRONG_PASSWORD_HERE
DB_NAME=cronny_production
# Path to database volume (DigitalOcean block storage mount)
DB_VOLUME_PATH=/mnt/cronny-production-db-volume/postgres

# ============================================
# API Configuration
# ============================================
# Frontend URL (for CORS)
FRONTEND_URL=https://example.com
# API URL (for frontend to call)
API_URL=https://example.com/api

# ============================================
# Authentication Configuration
# ============================================
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGEME_GENERATE_RANDOM_JWT_SECRET

# Google OAuth (get from https://console.cloud.google.com)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# ============================================
# SSL/TLS Configuration
# ============================================
# Email for Let's Encrypt notifications
CERTBOT_EMAIL=admin@example.com

# ============================================
# Docker Configuration
# ============================================
# Docker socket for Docker job execution
DOCKER_HOST=unix:///var/run/docker.sock

# ============================================
# Additional Services (Optional)
# ============================================
# Slack webhook for notifications (if using Slack actions)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Monitoring/Logging (optional)
# SENTRY_DSN=https://your-sentry-dsn
# LOG_LEVEL=info
